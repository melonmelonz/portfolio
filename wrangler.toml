name = "penn-portfolio"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

# Email Routing send binding. Restricted to the verified destination
# (lushfund@protonmail.ch is already verified on goolz.org), so the
# booking Function can email Penn without any API keys.
[[send_email]]
name = "EMAIL"
destination_address = "lushfund@protonmail.ch"
